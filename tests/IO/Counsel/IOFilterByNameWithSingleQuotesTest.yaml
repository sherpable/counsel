test:
  Counsel CLI IO filter by name feature with single quotes
perform:
  src/counsel.js --filter 'Dot reporter error true and false output' --as-io-test
expect: |
  Counsel {{version}}

  .

  Time: {{formattedExecutionTime}}

  9 passed, 1 tests
assertions:
  status: 0
